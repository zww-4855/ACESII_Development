
c To get a checksum, remove this file and use the routine in tools.

      subroutine checksum(name,a,n)
      implicit double precision(a-h,o-z)
      character*8 name
      dimension a(n)
      return
      end

